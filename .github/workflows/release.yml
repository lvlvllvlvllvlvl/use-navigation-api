name: Release Me

on:
  workflow_dispatch:
    inputs:
      bump:
        description: Version bump to apply
        type: choice
        options:
          - patch
          - minor
          - major
        required: true

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
      packages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 25
          registry-url: 'https://npm.pkg.github.com'
          scope: '@lvlvllvlvllvlvl'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build --workspace @lvlvllvlvllvlvl/use-navigation-api

      - name: Bump package version
        run: npm version "${{ inputs.bump }}" --workspace @lvlvllvlvllvlvl/use-navigation-api

      - name: Publish to GitHub Packages
        run: npm publish --workspace @lvlvllvlvllvlvl/use-navigation-api
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit and tag release
        run: |
          VERSION="$(node -p "require('./packages/use-navigation-api/package.json').version")"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add packages/use-navigation-api/package.json package-lock.json
          git commit -m "chore(release): v${VERSION}"
          git tag "v${VERSION}"
          git push origin HEAD
          git push origin "v${VERSION}"
