{
  "name": "use-navigation-api-monorepo",
  "private": true,
  "workspaces": [
    "packages/use-navigation-api",
    "packages/tests"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.3",
    "vite-tsconfig-paths": "^6.0.5"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
